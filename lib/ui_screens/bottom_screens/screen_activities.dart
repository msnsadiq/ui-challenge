import 'package:flutter/material.dart';

class ScreenActivities extends StatelessWidget {
  const ScreenActivities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.purple,
    );
  }
}
